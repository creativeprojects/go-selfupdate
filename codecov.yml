codecov:
  notify:
    after_n_builds: 6

comment:
  after_n_builds: 6

coverage:
  round: nearest
  status:
    project:
      default:
        target: auto
        threshold: "2%"
    patch:
      default:
        target: "70%"
        threshold: "2%"
